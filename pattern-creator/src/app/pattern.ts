export class Pattern {
   // id: number;
   // name: string;
    height: number;
    width: number;
    minSize: number;
    maxSize: number;
    cells: Cell[][];
    //mainColor: string;
    //backgroungColor: string;

}

export class Cell {
    color: number;
}